# Chart.yaml

apiVersion: v2
name: my-app
description: This chart deploys a complete application stack consisting of Nginx, MySQL, and Httpd.
version: 1.0.0

maintainers:
  - name : iftachzilca
    email: iftach.zilka7@gmail.com

#====================================================================================================================

# Detailed description of the application components:

  ## Nginx:
    #*** Configurable with custom html web page, with a nice html code.

  ## MySQL:
    #*** Provides a relational database for storing data.
    #*** Supports multiple databases and tables with defined schema and access controls.
    #*** Have a schema that called "mydata" and table that called "friends"

  ## Httpd:
    #*** Serves static content and dynamic pages generated by the application.
    #*** Configurable with modules and directives to customize its behavior.

#====================================================================================================================

# notes:

  #*** For seen the schema and table => inside the relevante pod with beginning name "mysql".
      # first: mysql -p
      # sec: show databases;
      # third: use mydata;
      # fourth: select * from friends;


    #*** nginx deployment have a configMap file with sweet html code inside that run with the deployment running.

#====================================================================================================================
# Resources:

  #*** Documentation: https://docs.github.com/
  #*** Source code: https://github.com/github


keywords:
  - nginx
  - mysql
  - httpd
  - 'web application'
  - 'helm chart'
  - deployment
  - database

# Keywords:
  # [ "nginx", "mysql", "httpd", "web application", "helm chart", "deployment", "database" ]